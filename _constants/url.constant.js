const {API_URL, HOMEPAGE_URL} = require('./config.json')
const APP_NAME = 'DimpleMe'

export const URL_CONSTANTS = {
    API_URL,
    APP_NAME,
    HOMEPAGE_URL,
    SURVEY_URL: `${HOMEPAGE_URL}/survey/`
};
